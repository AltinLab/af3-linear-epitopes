name: linear-epitope
channels:
  - conda-forge
  - bioconda
dependencies:
  - pip
  - python

  # jup
  - jupytext
  - ipykernel

  # for pandas interop
  - pyarrow
  - pandas

  # for uniprot API
  - requests

  # plotting
  - matplotlib

  # fasta parsing
  - biopython
# scikit-learn
  - scikit-learn
  - pip:
    # swap me for git URL
    - -e /scratch/sromero/af3-linear-epitopes
    - git+https://github.com/ljwoods2/mdaf3.git@main
